const gistDetails = (gistId) => {
    console.log(`The gist with ID ${gistId} has following details: null`);
};

const listUserGists = (userName) => {
    console.log(`User ${userName} has following gists list: null`);
};

module.exports = {
    gistDetails,
    listUserGists
};