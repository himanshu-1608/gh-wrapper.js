const orgDetails = (orgName) => {
    console.log(`Organization ${orgName} has following details: null`);
}

const userOrgs = (userName) => {
    console.log(`User ${userName} is a part of the following organisations: null`);
}

const listOrgMembers = (orgName) => {
    console.log(`Organization ${orgName} has following members: null`);
}

module.exports = {
    orgDetails,
    userOrgs,
    listOrgMembers
}