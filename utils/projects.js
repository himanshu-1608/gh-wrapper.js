const listOrgProjects = (orgName) => {
    console.log(`Organization ${orgName} has following projects: null`);
};

const listUserProjects = (userName) => {
    console.log(`User ${userName} has following projects: null`);
};

module.exports = {
    listOrgProjects,
    listUserProjects
};